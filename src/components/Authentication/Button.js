import React from 'react'

const Button = ({title}) => {
  return (
    <button className='mt-[29px] bg-primary-bg border-[1px] rounded-[10px] w-full border-black py-[22px] text-[40px] leading-6'>{title}</button>

  )
}

export default Button